/* global ol:false, proj4:false, window*/
(function() {
    'use strict';

    angular
        .module('starter-app')
        .constant('ol', window.ol)
        .constant('proj4', window.proj4);

})();